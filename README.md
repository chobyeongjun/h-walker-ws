# H-Walker Workspace
Cable-driven walker-mounted gait rehabilitation robot.

## Quick Start
- C++: `cmake -B build && cmake --build build`
- Python: `pip install -r requirements.txt`
- Teensy: `cd firmware && pio run -t upload`
- Run: `python src/hw_treadmill/main.py`
